{
  "name": "wherenpm",
  "version": "0.0.1",
  "description": "Get the global npm prefix. Zero dependencies.",
  "main": "dist/wherenpm.js",
  "types": "dist/wherenpm.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["npm", "prefix", "global", "where", "utils"],
  "author": "0xA672",
  "license": "MIT",
  "engines": {
    "node": ">=14"
  },  
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.0.0"
  }
}
